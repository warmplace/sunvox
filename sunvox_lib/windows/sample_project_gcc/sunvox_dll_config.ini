// Sound buffer size (in frames):
buffer 4096

// ALSA audio device name in Linux; or audio device number (from 0) in Windows:
//audiodevice hw:0,0
//audiodevice 0

// Audio driver name (alsa, oss, sdl, dsound, mmsound, asio):
//audiodriver alsa

// Sampling frequency (minimum - 44100):
frequency 44100